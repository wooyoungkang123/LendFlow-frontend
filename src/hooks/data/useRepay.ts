import { useState } from "react"; export function useRepay() { const [isPending, setIsPending] = useState(false); const [isSuccess, setIsSuccess] = useState(false); const [error, setError] = useState(null); const borrowBalance = 50n; const repay = async (amount) => { setIsPending(true); console.log(`Repaying ${amount}`); await new Promise(r => setTimeout(r, 1000)); setIsPending(false); setIsSuccess(true); }; return { repay, borrowBalance, isPending, isSuccess, error, refetch: () => {} }; }
